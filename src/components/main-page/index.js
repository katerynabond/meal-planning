const controller = require('./main-page.controller.js');
const template = require('./main-page.html');

const mainPageComponent = {
    controller: controller,
    template: template,
};

module.exports = mainPageComponent;