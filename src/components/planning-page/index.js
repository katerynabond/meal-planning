const controller = require('./planning-page.controller.js');
const template = require('./planning-page.html');

const PlanningPageComponent = {
    controller: controller,
    template: template
};

module.exports = PlanningPageComponent;